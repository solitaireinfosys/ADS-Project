'use strict';

/* Controllers */

angular.module('app')
    .controller('InvoiceCtrl', ['$scope', '$location', '$window', '$filter',
        function($scope, $location, $window, $filter) {
          
        }
    ]);
